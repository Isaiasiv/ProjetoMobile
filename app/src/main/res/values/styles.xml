<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ContainerComponents">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">250dp</item>
        <item name="background">@drawable/container_components</item>
        <item name="android:layout_margin">20dp</item>
    </style >

    <style name="BolhaDeCaixa">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/edit_caixa_de_entrada</item>
        <item name="android:padding">20dp</item>
        <item name="android:layout_marginLeft">40dp</item>
        <item name="android:layout_marginRight">40dp</item>
        <item name="android:layout_marginTop">15dp</item>

    </style>
    
    <style name="button">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginLeft">40dp</item>
        <item name="android:layout_marginRight">40dp</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:padding">20dp</item>
        <item name="android:background">@drawable/button</item>
        <item name="android:textSize">18sp</item>
        <item name="textAllCaps">false</item>

    </style>

    <style name="buttonPequeno">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:padding">20dp</item>
        <item name="android:background">@drawable/button</item>
        <item name="android:textSize">18sp</item>
        <item name="textAllCaps">false</item>

    </style>

    <!-- Estilo para títulos -->
    <style name="TitleStyle">
        <!-- Cor do texto -->
        <item name="android:textColor">#000000</item>
        <!-- Tamanho da fonte -->
        <item name="android:textSize">24sp</item>
        <!-- Peso da fonte (negrito) -->
        <item name="android:textStyle">bold</item>
        <!-- Centralizar o texto -->
        <item name="android:gravity">center</item>
        <!-- Margem opcional -->
        <item name="android:layout_margin">16dp</item>
    </style>

    <!-- Estilo para o texto acima da caixa de entrada -->
    <style name="LabelStyle">
        <!-- Cor do texto -->
        <item name="android:textColor">@color/white</item> <!-- Cinza médio -->
        <!-- Tamanho do texto -->
        <item name="android:textSize">14sp</item>
        <!-- Peso da fonte -->
        <item name="android:textStyle">normal</item>
        <!-- Espaçamento entre linhas, para clareza -->
        <item name="android:lineSpacingExtra">2dp</item>
        <!-- Margem opcional -->

        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_margin">15dp</item>
    </style>




</resources>